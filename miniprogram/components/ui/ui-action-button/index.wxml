<button
  type="{{btnType}}"
  size="{{size}}"
  loading="{{loading}}"
  disabled="{{disabled || loading}}"
  bindtap="onTap"
  class="ui-action-btn {{loading ? 'ui-action-btn--loading' : ''}} {{disabled ? 'ui-action-btn--disabled' : ''}} {{extraClass}}"
>
  {{text}}
</button>
