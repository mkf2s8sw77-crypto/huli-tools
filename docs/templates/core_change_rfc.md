# RFC: [标题]

> 状态: 草稿 / 审核中 / 已批准 / 已实施 / 已废弃
> 作者:
> 日期:
> RFC 编号: RFC-XXXX

## 1. 背景与问题

为什么需要做这个变更？当前存在什么问题或限制？

## 2. 涉及的公共云函数 / Action

| 云函数 | Action | 变更类型（新增/修改/删除） |
|---|---|---|
| `coreXxx` | `xxxAction` | 修改 |

## 3. 涉及的 Collection / Schema

| 集合 | 变更内容 |
|---|---|
| `xxx` | 新增字段 `fieldName` (类型, 默认值) |

## 4. 是否向后兼容

- [ ] 是，现有调用方无需修改
- [ ] 否，需要以下调用方同步修改：
  - 调用方 1:
  - 调用方 2:

## 5. 数据迁移方案

已有数据是否需要迁移？如何迁移？

- 迁移脚本位置:
- 迁移步骤:
- 是否可回滚:

## 6. 安全影响

- 是否涉及 `_internalToken` 校验逻辑变更？
- 是否涉及权限模型变更？
- 是否涉及支付链路变更？
- 是否可能导致客户端直接访问敏感数据？

## 7. 幂等与失败恢复

- 变更后的写操作是否仍保持幂等？
- 失败路径是否返回稳定错误码？
- 状态机流转是否仍保持一致？

## 8. 对现有应用的影响

列出所有受影响的业务应用及具体影响：

| 应用 (appKey) | 影响描述 | 是否需要同步修改 |
|---|---|---|
| `demo_sum` | 无影响 | 否 |

## 9. 测试计划

- [ ] 单元测试覆盖变更逻辑
- [ ] 受影响应用的冒烟测试
- [ ] 幂等性验证
- [ ] 异常路径验证
- [ ] 权限边界验证

## 10. 回滚方案

如果变更出现问题，如何回滚？

- 云函数回滚:
- 数据回滚:
- 预计回滚时间:

## 11. 文档更新

变更实施后需同步更新的文档：

- [ ] `AGENTS.md`
- [ ] `docs/cloud_collections.md`
- [ ] `docs/app_boundary_and_onboarding.md`
- [ ] `test_case_huli-tools_0526.md`
- [ ] `run_manifest_huli-tools_0526.toml`
- [ ] 其他:

## 12. 审批记录

| 审批人 | 日期 | 结论 |
|---|---|---|
| | | |
